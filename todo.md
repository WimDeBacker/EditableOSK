# OnScreenKeyboard – Todo List

## Pending

### Other pending work

- [x] **Priority 1 — Shrink and review the test suite** ✓ *(maintenance, completed 2026-09-19)* — The test program has grown large. Audit which tests are load-bearing, which are redundant duplicates, and which are deprecated. Goal: smaller, faster, easier to maintain suite without losing meaningful coverage.
  - **Done (2026-09-19):**
    - **A — silent gaps closed.** The end-to-end prediction suite (`WordPredictorE2ETests`, ~110 assertions) never ran: it looked for the retired `worddb.wfq`. It now uses `worddb_NL.wfq`; two stale expectations were corrected (`E2E-11`, `E2E-14`). A missing `worddb_NL.wfq` or `lang_nl.xml` is now a failure, not a skip. Both prediction suites load a temp copy of the database, so a `*.learned.wfq` left behind by running the app can no longer change the results.
    - **B4 — `BuildDefaultQwerty`:** ~490 per-cell assertions collapsed into six `AssertAll` checks that name the first offending cell.
    - **B5 — duplicates:** `KeyEditorForm.ToHuman`/`FromHuman` and `KeyboardForm.StripSendBraces` are now `internal`, and the tests call them instead of two/one hand-copied versions. `EscapeForSend`, `IsPlainText` (incl. the win: cases and the routing spot-checks) each have a single test.
    - **B6 — translation text:** identity checks (`Lang.T("X") == "X"`) and hard-coded wording replaced with structural checks: every string has at most one accelerator, `{n}` placeholders match between English and Dutch, every `tip:` key is registered and translated. Verified by mutation (broken placeholder, missing tip, double `&` all fail).
    - **B7 — theme presets:** `NewKeyboardWizard.Presets` is now `internal`; `T_WizardThemePresets` checks the real data for all four presets (it used to check typed-in copies, and never looked at Colorful): valid hex, all six groups present, font-on-key / font-on-background contrast per preset, and every group's contrast. Verified by mutation.
    - **Undo/redo:** the tests exercised a copy of the stack logic. The state machine is extracted into `UndoHistory<T>` (new `UndoHistory.cs`), which `KeyboardForm` now uses (`_history`, `CaptureSnapshot`); the tests drive the real class (cap of 50 and ordering, undo/redo round trip, a new edit clears redo, real snapshot content). Verified by mutation.
    - Suite: 1,778 → 1,306 assertions, all passing, ~4 s.
  - **Found while doing B7 (not fixed):** the Colorful preset is below WCAG AA (4.5:1) for normal text in two groups — Klinkers (white on `4A8FD4`, 3.4:1) and Leestekens (white on `C86A00`, 3.8:1). The colours come from the azertycolor design; the test pins a 3:1 floor until they are reconsidered.
  - **Not done (deferred, optional follow-ups):** B8 (shared helper for the three editor dialogs), C (split the 5,800-line file by area, break up `T_StyleGroups` and the two word-database runners, rename stale "Priority N"/"Step N" section titles, runner improvements), and making the prediction tests independent of the shipped Dutch dictionary.

- [ ] **Priority 2 — Touch-friendly controls and sectioned editor windows** *(accessibility / UI)* — Merged from the former Priorities 2 (touch target sizes) and 5 (editor windows too tall), because both change the size of the editor windows: bigger targets make windows taller, so the two must be designed together. **Touch-friendly sizing is the default on every desktop, not a setting** — the software must be easy to use for people with a motor disability.
  - **Targets:** every clickable control (buttons, colour swatches, the `+` group button, checkboxes, combo boxes, spin buttons, tab/section buttons) is at least 44 × 44 px (WCAG 2.5.5). Today swatches and the `+` button are 32 × 26 px, standard buttons are 34 px (`Fluent.BtnH`), and the spin buttons of `NumericUpDown` are only a few pixels wide — consider replacing them with `[−] value [+]` controls with 44 px buttons.
  - **Windows too tall:** Key Editor (~770 px tall, 1,080 px wide before any enlargement) and Group Editor do not fit small or scaled displays; today they only scroll. Reorganise into sections (tabs or a segmented section bar) so each section fits the screen at 44 px targets; pin the live preview and Apply/Cancel so they are always visible.
  - **Layout approach — flow/table layout, not hand-positioned controls:** build each section with `TableLayoutPanel` (rows/columns sized `AutoSize`/`Percent`) and `FlowLayoutPanel` (wrapping button rows, e.g. the five mode buttons) inside the existing `AddGroup` cards, instead of computing `Left`/`Top` pixel positions (`gy += ROW_H`). Larger targets, larger fonts and other DPI settings then fit without recomputing coordinates, and a narrower window reflows.
  - **Languages (label lengths differ):** the current layout reserves a fixed label column with a lot of empty space to absorb longer translations (Dutch is longer than English; other languages may be longer still). Flow/table layout makes that space unnecessary: `AutoSize` labels let the label column size to the longest label of the language in use, and long labels wrap (`MaximumSize`) instead of being cut off. If a label column would still become too wide, put the label above its input (a stacked layout needs no label column at all). Never rely on a fixed pixel width for text.
  - **Language test:** add a pseudo-localisation test — load every dialog with each string expanded by ~40% and assert no label or button text is clipped (measured text width/height with `TextRenderer.MeasureText` versus the control size) and no control overflows its section. This replaces "leave lots of empty space" with a checked guarantee.
  - **Dynamic window size:** the editor windows must size themselves from their content instead of using a constant (today `Key Editor` 1080 wide, `Group Editor` 880 × 610, `Keyboard Editor` 940 × 560, wizard 880 × 820, passed to `FluentDialogBase(Size)`). Target behaviour: the preferred size comes from the content (`AutoSize`/`GetPreferredSize` of the section layout, recomputed when the language, font size or DPI changes); it is clamped to the screen's working area (`MaximumSize`) and to a sensible minimum; the user can still resize, and the layout reflows to the new size (`Dock = Fill` sections) instead of leaving controls where they were; content that still does not fit scrolls. The sub-dialogs in `GroupEditorForm` that are `FixedSingle` get the same treatment.
  - **Consequences to handle:** validation errors on a hidden section must switch to it (and mark the section); accelerator uniqueness per section/dialog; tab order and arrow-key navigation between sections; layouts are hand-positioned, so each dialog is reworked once, in a single pass.
  - **Tests:** extend the accessibility tests to assert a minimum size for every interactive control in the editor dialogs.
  - **Progress (2026-09-19) — Steps 0–1 of the redesign done (foundation, no existing dialog changed yet):**
    - `TouchControls.cs`: `TouchTextBox`, `TouchComboBox`, `TouchCheckBox` (same base types, 44 px), `TouchStepper` (`[−] value [+]`, hold-to-repeat, replaces `NumericUpDown`), `SectionBar` (custom segmented buttons, arrow keys, warning badge), `SectionHost`.
    - `FluentDialogBase`: content-sized mode (parameterless constructor + `BuildFrame`), `AddSection`/`AddRow`/`AddWideRow`/`MakeFooter`/`MakeColorRow`, `ShowFirstSectionWithError` (jumps to the section with the error), Ctrl+Tab between sections, grows (never shrinks) after a language change.
    - `Lang.PseudoExpansion` (make every string N % longer) for layout tests; Dutch strings for the new controls.
    - `--gallery <folder>` saves PNGs of the sample dialog and the three existing editors in English, Dutch and +40 % text. `TouchSpikeForm` is the sample dialog (mirrors the planned Key Editor: 3 sections).
    - `UiGuardTests.cs`: guards for target size, clipped text, overflow and fit on a 1366 × 768 screen (strict on the sample dialog in English, Dutch, +40 % and +80 %), plus tests for every new control; the existing editors are only reported (`ui_guard_report.txt`): Key Editor 29 of 36 pointer controls under 44 px, Group Editor 17 of 17.
    - Verified by mutation (44 px text box, jump-to-error).
    - **Checkpoint A:** review the gallery screenshots, then Step 2 (Key Editor).
  - **Standard for every window: WCAG 2.1 AAA** (the software is for people with a motor disability, and low vision is common with it). Concretely: text >= 7 : 1 (1.4.6), boundary of every control and the focus indicator >= 3 : 1 (1.4.11), pointer targets >= 44 x 44 (2.5.5), full keyboard operation (2.1.3); checked in light AND dark theme. Disabled text is exempt. Wrong colours are caught by `T_ColourContrastAaa`; the gallery shows both themes (`mockup_E*_light_*.png`, `mockup_E*_dark_*.png`).
  - **Done (2026-09-19) — AAA palette.** Every text and control colour token was under AAA (white on the blue/green/red buttons 4.5–5.7 : 1, hint/secondary text 5.3–6.5 : 1, neutral button border 1.3 : 1, dark-theme hint/description text 1.9–5 : 1). Changed in `FluentTheme.cs`: `Accent` (0,84,150), `Success` (10,100,10), `Danger` (150,30,20) (+ their pressed variants), `TextSecondary`, `TextHint`; new `ControlBorder` / `ControlBorderHover` for every control boundary (the pale `BorderCard` / `BorderInput` greys are for grouping lines only); new dark-dialog tokens `DialogDark*` replace the colours that were typed in several places. `FluentPainter.PaintLight`: neutral buttons get a real border; the dark coloured buttons get a light outline on a dark parent (otherwise they vanish into it). Visible effect: primary blue, Apply green and error red are darker in the whole app.
  - **Not yet audited for AAA:** the main keyboard toolbar (`Dark*` palette, `DarkTextDim`), the wizard's own pages, the existing editors' hand-typed colours (`KeyEditorForm`, `GroupEditorForm`, `KeyboardEditorForm` still use their own `C_*` tokens, mostly the shared ones), and the wizard theme presets (Colorful is below AA: Klinkers 3.4 : 1, Leestekens 3.8 : 1; AAA for preset colours is a design decision for the keyboard itself, not for the dialogs).
  - **Decided Key Editor design ("Option E", mock-up in `KeyEditorMockups.cs`, screenshots via `--gallery`):**
    - **Two sections** — *Key Content* and *Appearance* — plus a **labelled preview card ("Preview") at the top right of both**, next to the section buttons; the footer holds only Cancel / Apply.
    - **Key Content:** a grid with one row per layer (Normal / Shift / AltGr) and the columns *Label* (narrow: a key label is short, about 11 characters) | *Action* = type chooser + value + contextual button (*Browse…* / *Record…*); then Key width / Key height side by side. "Send" is renamed **Action**. The five mode buttons are gone: the action type is a `TouchChoiceButton`. Only Text, Key/Shortcut and Layout work per layer today; **Modifier** and **Word prediction** belong to the whole key (a key is a modifier by its *label*, a prediction slot by its main send `wp:`), so on Shift/AltGr they stay in the list, greyed out with the reason ("Whole key only: set it on the Normal layer"). This also replaces the "last focused field" trick of the shared Browse button.
    - **Appearance:** Group (chooser + *Manage Groups…*), Font (chooser with search), Font size + Auto, **Colours as one row of three labelled chips** (Font / Key / Border; hex is not shown), Border thickness.
    - **`TouchChoiceButton`** (replaces `ComboBox` everywhere in the editors): a >= 44 px button showing the choice, tap opens a flyout with 44 px rows (56 px with a description line). For lists taller than the screen: height limited to the room above/below the button and to 8 rows, two 44 px arrow strips scroll (press and hold repeats; wheel and keyboard work too), and a search box filters (`Searchable`, for fonts). Keyboard: Enter/Space/Alt+Down open, Up/Down change without opening, Esc closes.
    - **Colour chooser:** Windows' `ColorDialog` has no hex box (only H/S/L and R/G/B), and the newer Windows picker with hex exists only in WinUI, so `ColorChip` opens our own flyout: a 24-swatch palette of 44 px (one tap picks), a hex box that applies while typing, and *More colours…* which opens the standard `ColorDialog`. The palette works with the arrow keys.
  - **Steps still to do:**
    1. **Key Editor migration** — build the real dialog from the pieces above (keep control field names used by tests where possible); validation errors jump to the section; accelerators unique per dialog incl. the section buttons; the "empty colour = inherit from group" state in the colour chips and choosers; all text in `Lang` (en + nl); tests: strict UI guards for the Key Editor (targets, clipped text incl. +40 % and +80 %, fits 1366 × 768, light and dark), keyboard operation of `TouchChoiceButton` / `ColorFlyout`, moves from the baseline report to the strict list. Delete the old positioning code.
    2. **Unit tests for the new components** — `TouchChoiceButton` (selection skips disabled rows, filter, scrolling limits, keyboard), `ColorFlyout` (hex parsing, palette selection, invalid code).
    3. **Group Editor and its sub-dialogs, Keyboard Editor (three sections: General, Accessibility, Word prediction), wizard** — same pattern. The Group Editor is master/detail (group list beside the style form), no tabs.
    4. **Narrow screens** — the Action row has five columns and fits 760 px; below that the value field must move under the label/action (stacked layout).
    5. **Cleanup** — remove `ROW_H` / `HDR_H` / `WrapInScrollPanel`, the old `AddColorRow`, `NumericUpDown` and `ComboBox` uses, `KeyEditorMockups.cs`; update the accelerator test to "unique per dialog".
    6. **Decide later:** the same flyout for every other list (Language, Toolbar theme, word-database choice); a shortcut builder (`Ctrl`/`Alt`/`Shift`/`Win` toggles + key) instead of "Record", for people who cannot type combinations.
  - **Order:** step 1 (Key Editor) first as the pattern, then 2, 3, 4, 5.

- [ ] **Priority 3 — Scanning** *(accessibility)* — Auto-advance a highlight through keys/rows at a fixed interval; the user confirms with a single switch input.

- [ ] **Priority 4 — Bundled keyboard layouts** *(content)* — Ship 8 ready-made `.kbl` files: 4 wizard themes × 2 key arrangements (AZERTY Dutch, QWERTY English). Users can load one immediately without running the wizard.

---

## Completed

### Word prediction — learning engine (overlay redesign) ✓

First cut used a full "personal copy" of the base `.wfq` (manual dropdown +
copy step, learned frequency mixed into the base corpus frequency). Replaced
after review — too much setup friction, and a +1 frequency bump is invisible
against base corpus frequencies in the thousands/millions. Redesigned around
a small overlay file and a separate personal-use ranking tier:

- [x] **Overlay file, not a full copy** ✓ — `worddb_NL.wfq` → `worddb_NL.learned.wfq` (same dir, `WordDatabase.DeriveOverlayPath`/`GetOverlayPath`). Holds only what changed: `<PersonalUse>`/`<PairUse>` deltas for base words/pairs, `<NewWord>`/`<NewPair>` for promoted candidates (with a `<NewWord>`'s own `<Next>` children nested inside it), and `<Candidates>`. `WordDatabase.Load` merges it onto the freshly-parsed base snapshot before publishing (`ApplyOverlay`), reusing the same mutation primitives `RecordWord` uses. `LanguageRegistry` excludes `*.learned.wfq` from its database scan.
- [x] **`RecordWord`** ✓ — Called from `WordPredictor.CompleteWord` on every finished word (typed or WP-clicked). Known words/pairs: `WordEntry`/`NextEntry.PersonalUseCount` incremented — kept separate from `Frequency` (the base corpus value), never blended. Unknown words go into a candidate buffer, promoted to a real word once seen more than twice. No-op unless `WordDatabase.LearningEnabled` (default **on**; no personal-file setup needed at all).
- [x] **Personal-use ranking tier** ✓ — `GetPredictionsCore` gained "Step 1.5": words/pairs with `PersonalUseCount > 0` are ranked ahead of raw frequency, capped at `PersonalCap(count)` (≈3/4 of the slots, always leaving ≥1 for a normal suggestion) so personal usage reliably surfaces without ever fully crowding out the base corpus.
- [x] **Sentence-start normalisation** ✓ — A word is only auto-capitalised because it opened a sentence (display artefact); `WordPredictor` tracks this per-word and learns it lowercase, while a genuine mid-sentence Shift press (proper noun) keeps its capital.
- [x] **Batched, crash-safe persistence** ✓ — `SaveIfDirty()`/`SaveNow()` (parameterless — always target the overlay paired with whatever base is loaded) build a plain-data copy on the caller's (UI) thread — so a background write never races the UI thread's in-place `RecordWord` mutations — then write via `Task.Run` using the same `.tmp` + `File.Replace` pattern as `SettingsManager`. `KeyboardForm` calls it from a 30 s timer and does a final synchronous `SaveNow` on close.
- [x] **"Remember typed words" toggle** ✓ — `LayoutMeta.WordLearningEnabled` (default true) replaces the old database-choosing dropdown/copy-dialog UI in Edit Keyboard → Word Prediction with a single checkbox; the database dropdown now only chooses between base language files (no personal/copy concept). Candidate list + Promote/Reject buttons gated on the toggle. Export now exports the (small) overlay file.
- [x] **Tests** ✓ — 1648/1648 passing: personal-use vs. frequency increments kept separate, candidate buffering and auto-promotion at count > 2, manual promote/reject, overlay merge-at-load (`<PersonalUse>`/`<NewWord>`/`<PairUse>`/`<NewPair>`/`<Candidates>`), personal-use ranking cap, save targets the overlay (never the base file), no-op when `LearningEnabled = false`, `WordPredictor` sentence-start-vs-proper-noun case normalisation, and `LanguageRegistry` excluding overlay files from its scan.

### Word prediction — multi-language & database ✓

- [x] **Step 1 — New file extensions** ✓ — Layouts `.xml` → `.kbl`; databases `.xml` → `.wfq`. All file pickers, csproj, tests, and physical files updated.
- [x] **Step 2 — `.wfq` format** ✓ — Root element gets `version`, `language`, `isPersonal` attributes and `<Candidates />` section. Build-time converter `tools/ConvertWordDb.ps1` produces `.wfq` from source `.xml`. `WordDatabase` exposes `Language` and `IsPersonal`.
- [x] **Step 3 — LanguageRegistry** ✓ — Scans app folder for `.wfq` files via fast `XmlReader` root-element peek; groups by language; sorts base before personal alphabetically.
- [x] **Step 4 — Layout → database link** ✓ — `LayoutMeta.WordDatabase` stores optional filename override. `LoadWordDatabase()` auto-selects: explicit file → language-matched base → any base.
- [x] **Step 5 — Database management UI** ✓ — Word Prediction card in Edit Keyboard: dropdown of all `.wfq` files; base files (`→`) trigger personal-copy dialog with revert-on-cancel; personal files (`★`) are the only selectable targets; `● unsaved` indicator when selection differs from saved value.
- [x] **Step 6 — Tests** ✓ — 1613/1613 passing. Covers: `.kbl`/`.wfq` round-trip, `.wfq` metadata, `WordCount`, `IsLoading`/`Loaded` event state, sequential reload, concurrent load stability (10-task stress), Dutch regression. UI combo and English regression not unit-testable (SaveFileDialog / worddb_EN.xml excluded from repo).

### Word prediction — fast background loading ✓

- [x] **XmlReader + background load** ✓ — `XmlDocument` replaced by forward-only `XmlReader` (~3–5× faster, ~1.5 GB → ~200 MB peak). `LoadWordDatabase()` uses `Task.Run`; keyboard opens immediately with blank WP cells. `WordDatabase.Loaded` event marshals back via `BeginInvoke` → `ApplyWPTags()`. `_lastLoadedDbPath` guard skips redundant reloads on settings-only Apply. Thread-safety: immutable `DbSnapshot` published via a single `volatile` write — no locking on read path. `worddb_EN.xml` excluded from git; size driven by depth (up to 1000 `<Next>` entries per word by design) and managed by the external database builder.

### Code review (ultra) — 8 findings fixed ✓

- [x] BeginInvoke TOCTOU: `try { BeginInvoke(…) } catch (InvalidOperationException)` in both `Loaded` handlers.
- [x] Concurrent Load() race: generation counter (`Interlocked.Increment` + `Volatile.Read`) — superseded loads abort before publishing.
- [x] `grpWP` missing from `WrapInScrollPanel` — added, card now scrolls correctly.
- [x] Partial `.wfq` file on copy failure — deleted in catch block before showing error.
- [x] `Loaded` subscriber exception swallowed — `Loaded?.Invoke()` wrapped in `try/catch`.
- [x] Editor info label stale during async reload — `KeyboardEditorForm` subscribes to `WordDatabase.Loaded` and refreshes `UpdateWPInfoLabel()` on completion.
- [x] Unnecessary reload on every Apply — `_lastLoadedDbPath` guard skips `Task.Run` when path unchanged.
- [x] `IsLoaded=true && IsLoading=true` window — `_isLoaded = true` moved after the `finally` block.

### Code review (ultra, 2026-08-12) — findings dismissed (not a bug)

- [x] ~~Priority 2 — Key editor silently reassigns a WP slot on open~~ — Investigated 2026-08-14: `KeyboardForm.NormaliseWPSlots()` (`KeyboardForm.cs:1092`) renumbers `wp:N` cells to a contiguous `0..N-1` run after every mutation that can touch WP membership (key removal, paste, editor Apply, and every layout load/rebuild). Given that invariant, `OpenEditor`'s `usedWpSlots` (all WP slots except the cell being edited) is always exactly `{0..N-1} \ {ownSlot}` — so `SetSendMode`'s "next free slot" scan always lands back on `ownSlot`. The non-contiguous state the finding describes (e.g. slot 5 with 0/2/5 used) can't exist when the editor opens, so the auto-assign is a no-op here in practice, not a data-loss bug.

### Code review (ultra, 2026-08-12) — findings fixed ✓

- [x] **Priority 1 — AutoSave silently drops edits on an invalid layout** ✓ — `SettingsManager.SaveSettings` gained an `allowInvalid` opt-in parameter (default `false`, preserving the existing throw-on-invalid behaviour for interactive Save/Save As and the wizard). `KeyboardForm.AutoSave()` now passes `allowInvalid: true` so it can no longer have its write silently swallowed by its own empty `catch {}` — an in-progress edit with a transiently invalid layout is preserved verbatim instead of being dropped until the layout becomes valid again. Regression test added to `T_SettingsManager_AtomicSave`: an invalid layout saved with `allowInvalid: true` writes successfully and round-trips with its (overlapping) cells intact, while the default (no `allowInvalid`) still throws.

- [x] **Priority 2 — Wizard "theme from file" didn't restyle a copied layout, and never classified pasted keys into groups** ✓ — `NewKeyboardWizard.BuildLayoutData`'s from-file theme branch (`NewKeyboardWizard.cs:1014`) now overwrites a matching group's `KeyColor`/`FontColor`/`BorderColor`/`BorderThickness`/`FontName`/`FontSize` from the theme file instead of skipping it when a group of that name already exists — fixes "copy azerty.kbl + theme from azertycolor.kbl" silently keeping azerty's original colors. `AutoClassifyLayout(layout)` was moved out of the built-in-preset-only branch to run unconditionally whenever `_rbPaste.Checked`, regardless of which theme source was picked — fixes pasted labels never getting grouped ("a"→Klinkers, "b"→Medeklinkers, etc.) when a from-file theme was chosen. Verified with a full build (0 warnings/errors) and the full test suite (1652/1652 passing), plus a new regression test `T_WizardBuildLayoutData_ThemeFileMerge` (`OnScreenKeyboardTests.cs:4688`) that drives the real `NewKeyboardWizard` via reflection and asserts on the actual `BuildLayoutData()` output.

- [x] **Priority 2b — Wizard theme-page example swatches stayed grey after choosing a theme file** ✓ — Found while manually verifying Priority 2: `_pnlThemePreview.Invalidate()` (`NewKeyboardWizard.cs`, `SelectPreset`) only fires once, when the "From file" button is first clicked — while `_txtThemeFile.Text` is still empty, so the swatches paint their default grey. Nothing repainted the panel afterward when the user actually picked a file via Browse or typed a path, since `_txtThemeFile` had no `TextChanged` handler at all. Fixed by wiring `_txtThemeFile.TextChanged += (s,e) => _pnlThemePreview?.Invalidate();` right after the Browse button's click handler (`NewKeyboardWizard.cs:588-596`), mirroring the existing `_txtPaste.TextChanged` pattern used for the grid preview. This is a repaint-timing bug, not a data bug — not practical to unit-test headlessly (WinForms `Invalidate()`/paint only fires with a created window handle and message pump), so it's verified by code inspection only; worth a quick visual check next time the wizard is run manually.

- [x] **Priority 2c — Wizard theme-page swatches all showed the same colour for a from-file theme** ✓ — Found immediately after 2b, once the swatches actually started repainting: `GetGroupKeyColor`/`GetGroupFontColor` (`NewKeyboardWizard.cs:868-894`) completely ignored their `groupName` parameter in the from-file branch, calling `LoadThemeColor(path,"KeyColor"/"FontColor",...)` which only reads the single global `/OnScreenKeyboard/Theme` attribute — never the per-`<Group Name="Klinkers">` entries nested inside it. So every example key (Klinkers, Medeklinkers, Cijfers, ...) rendered with the identical global colour. Added `LoadThemeGroupColor(path, groupName, attr, fallback)` (`NewKeyboardWizard.cs:964-990`), which looks up the matching `<Theme><Group Name="...">` node and falls back to the file's global colour if that group isn't present — same fallback shape the built-in-preset branch already used. Verified with a new regression test `T_WizardThemePage_GroupSwatchColors` (`OnScreenKeyboardTests.cs:4756`): with `azertycolor.kbl` as the theme file, Klinkers now resolves to its own `#4A8FD4` and Medeklinkers to `#1A4E8A` instead of both collapsing to the global colour. Full suite: 1660/1660 passing.

- [x] **Priority 2 — New Keyboard Wizard can finish with an empty or wrong layout** ✓ — `NewKeyboardWizard.ValidatePage` (`NewKeyboardWizard.cs:681`) now blocks navigation past the start page if "copy from" is selected and the path fails `File.Exists` (previously only checked for a non-blank string, so a typo'd path silently fell through to a blank 1×1 layout in `BuildLayoutData`). Also added a new check on the grid/paste page: if "paste" is selected and `WizardKeyParser.Parse` yields zero rows, Next is blocked with a new `wiz: err empty paste` message (added to `LanguageManager.cs` and `lang_nl.xml`) instead of silently producing a gear-only layout.

- [x] **Priority 2 — Case-mismatch breaks protected "standard" group on import** ✓ — Root cause in `GroupEditorForm.ApplyImportDecisions` (`GroupEditorForm.cs:1106`): `Overwrite`/`UpdateStandard` matched the target group case-insensitively but then replaced the whole entry — including `Name` — with the imported group's clone, adopting the import file's casing verbatim (e.g. "Standard"). Every other lookup in the app (`SettingsManager.cs`, `KeyEditorForm.cs`, `KeyboardForm.cs`, `NewKeyboardWizard.cs`) compares `StandardGroupName` with plain ordinal `==`, so the renamed entry silently lost its protected status and a fresh lowercase "standard" group got synthesized on next load. Fixed by keeping the local entry's existing `Name` on Overwrite/UpdateStandard — only the style fields (`KeyColor`/`FontColor`/`BorderColor`/`BorderThickness`/`FontName`/`FontSize`) come from the import; renaming was never the intent of "Overwrite." Folded in a related gap noticed while reviewing this: manually adding or renaming a group only ever checked against the single reserved word "standard" (`IsReservedGroupName`), not against a case-insensitive collision with *any* other existing group — so adding "klinkers" when "Klinkers" already existed created two groups that every case-sensitive lookup elsewhere treats as unrelated. Added a `NameCollides(name, excludeIdx)` helper and wired it into every write path: the Add-group dialog (`GetNewName()`), the rename text box's live validation (`SaveCurrentName`) and its authoritative write (`CommitTo`), and the two test helpers `TryAddGroup`/`TryRenameCurrentGroup`. New lang key `"A group with this name already exists."` added to `LanguageManager.cs` and `lang_nl.xml`. Verified with 11 new assertions across two new test blocks in `T_StyleGroups` (`OnScreenKeyboardTests.cs`) — a case-mismatched "Standard" import no longer renames or duplicates the local group, and add/rename correctly reject case-insensitive collisions while still accepting distinct names. Full suite: 1671/1671 passing.

- [x] **Priority 1 — A key's font silently detached it from its group whenever the group's font wasn't installed** ✓ — Investigated live using the user's own `new keyboard.kbl`, whose global theme references a custom font (`SchoolKX_New`) not installed on the machine editing it. Root cause: `_cmbFont` in both `GroupEditorForm` and `KeyEditorForm` is a closed `DropDownList` populated only from `Fluent.InstalledFontNames()` — when the resolved font isn't in that list, `IndexOf` fails and both forms fell back to index 0 (`KeyEditorForm`: whatever's alphabetically first; `GroupEditorForm`: the "(inherit standard)" placeholder). `GroupEditorForm.CommitTo` then silently cleared the group's real `FontName` to `""` on any Apply/OK, even with zero user interaction. `KeyEditorForm.Apply`'s auto-detach heuristic compared the combo's forced-fallback display value against `_groupFontName`, saw a mismatch, and misread it as a deliberate edit — silently detaching the key from its group (`GroupName=""`) and baking in the wrong font, while colors looked fine since they round-trip through hex with a genuine "not set" escape hatch that the font check never had. Fixed with two changes: (1) `FluentDialogBase.SelectOrInsertFont` (`FluentDialogBase.cs`) preserves an uninstalled font name by inserting it into the combo (at index 1, never colliding with a reserved "(inherit standard)"/"(none)" placeholder at index 0) instead of silently substituting; wired into both `GroupEditorForm.LoadDetail` and `KeyEditorForm.RefreshAppearanceFromGroupCore`. (2) `KeyEditorForm` gained a `_fontUserChanged` dirty flag — set only by a genuine `_cmbFont.SelectedIndexChanged` outside `_initialising`, reset on every group switch — replacing the value-comparison in `Apply()`'s `anyChanged` check, so the class of "the display can't represent the true value" bugs (not just this one) can no longer masquerade as a real edit. Also added: `KeyboardForm.GetMissingFonts`/`WarnIfFontsMissing`, called after every layout load (`ApplyLoadedSettings`, `TryAutoLoad`), warns the user by name when a loaded file references a font that isn't installed, suggesting they search online for it (new lang keys `"font missing title"`/`"font missing msg"`). Cleaned up `qwerty.kbl`, which hard-coded `FontName="Consolas"` on 67 keys despite its own theme already being Arial — cleared to `""` (inherit) to match `azerty.kbl`'s existing convention; `azerty.kbl` itself was already Arial-only, no change needed. Bundled the actual `SchoolKX_New.ttf` (supplied by the user) into `installer\fonts\` and added an Inno Setup `[Files]` entry with `FontInstall: "SchoolKX_New"` (`installer\setup.iss`) so `azertycolor.kbl`'s theme font installs automatically on a fresh machine — not compile-tested since Inno Setup isn't installed in this environment; worth a real `iscc` build before shipping. Verified with 12 new assertions in `T_MissingFontHandling` (`OnScreenKeyboardTests.cs`) covering `GetMissingFonts`, `SelectOrInsertFont`, and end-to-end `GroupEditorForm`/`KeyEditorForm` round-trips. Full suite: 1704/1704 passing.
  Follow-up: the font-preservation fix left no in-context signal that a selected font isn't installed (only the load-time warning). Added `FluentDialogBase._fontWarn` — a second `ErrorProvider` (warning-triangle icon, distinct from `_err`) shown next to `_cmbFont` via `UpdateFontAvailabilityWarning(combo, fontName)` whenever the currently selected font isn't installed. Deliberately a separate provider from `_err` so it's purely informational and never counts toward `HasPendingErrors()` — an uninstalled font is a fact worth surfacing, not a reason to block Apply/OK. Wired into `GroupEditorForm` (initial `_cmbFont` construction's `SelectedIndexChanged`, plus `LoadDetail`) and `KeyEditorForm` (`RefreshAppearanceFromGroupCore`, plus the existing `SelectedIndexChanged` handler). 4 more assertions added to `T_MissingFontHandling`. Full suite: 1708/1708 passing.
  Follow-up: the warning icon (`SystemIcons.Warning` at native size) rendered too large next to the combo box. Sized down 10% via `new Icon(SystemIcons.Warning, w*0.9, h*0.9)`, stored in a new owned `_fontWarnIcon` field (unlike the shared `SystemIcons.Warning`, this copy must be disposed) and disposed alongside `_tip`/`_err`/`_fontWarn` on `FormClosed`. Full suite: 1708/1708 passing.

- [x] **Priority 2 — Hex-color validation is cosmetic only in four editor forms** ✓ — Implemented earlier in this session but never logged here at the time (found stale/undocumented when the user asked to double-check the Pending list against actual code state). `FluentDialogBase.HasPendingErrors(Control root = null)` walks the control tree checking `_err.GetError(c)`, and is now called at the top of all three forms' Apply/OK: `GroupEditorForm.cs:465`, `KeyEditorForm.cs:1894`, `KeyboardEditorForm.cs:1024` — each refuses to proceed while any field is flagged invalid, making the existing `ErrorProvider` icon actually block instead of being decorative. `KeyboardEditorForm`'s worst-case hardcoded `#1A1A2E` fallback was also replaced with `_srcTheme.BackgroundColor` (the prior value) as defense-in-depth. Verified via `T_ValidationBlocksApply` (`OnScreenKeyboardTests.cs`). Confirmed still in place by direct code inspection 2026-08-14 (not re-tested, since no code changed).

- [x] **Priority 1 — Wizard "Copy from file" + a built-in preset theme (e.g. High Contrast) doesn't restyle the copied groups** ✓ — Confirmed by re-reading `NewKeyboardWizard.ApplyPreset` (`NewKeyboardWizard.cs:1131`) directly — the exact bug the finding described, unaffected by the earlier from-file fix which only touched the sibling branch. Same fix shape as that earlier one: `ApplyPreset`'s `foreach` over `p.ExtraGroups` now finds an existing group by name and overwrites its `KeyColor`/`FontColor`/`BorderColor`/`BorderThickness` from the preset, instead of only adding a new `KeyGroup` when `!layout.Groups.Exists(...)` and silently leaving a same-named pre-existing group (e.g. a copied `azerty.kbl`'s `Klinkers`/`Medeklinkers`/etc.) at its original colors. No-op for Blank/Paste starting points, where `layout.Groups` is still empty when `ApplyPreset` runs — `existing` is always `null` there, so behavior for those paths is unchanged. Verified with a new regression test added to `T_WizardBuildLayoutData_ThemeFileMerge` (`OnScreenKeyboardTests.cs`): copying `azerty.kbl` and picking the High Contrast preset now gives `Medeklinkers` the preset's `#FFE535`, not azerty's own `#3E6C60`. Full suite: 1710/1710 passing.

- [x] **Priority 1 — Learned overlay word can clobber a real dictionary entry** ✓ — User questioned whether this still applied given the overlay/base file separation; re-read the code to confirm it does — separation protects the base *file* from being written to, not the in-memory *merge* every load performs. `WordDatabase.ApplyOverlay`'s `case "NewWord"` (`WordDatabase.cs:515`) did `snap.ByExact[value] = entry` unconditionally, with no check for whether a real base entry already existed there. Trigger: a word gets personally promoted into the overlay while absent from the base; the base dictionary is later updated to legitimately include that same word (real corpus frequency + bigram data); on next load the overlay's stale, much sparser `<NewWord>` record silently overwrites the just-loaded rich base entry. A second wrinkle found while fixing it: `snap.ByFrequency.Add(entry)` added a new entry object without removing the old base one from that list, an orphaned-duplicate risk on collision. Fixed by checking `snap.ByExact.TryGetValue(value, out var existing) && existing.IsFromBase` first — on a real collision, folds the overlay's `PersonalUseCount` into the existing base entry (mirroring how `<PersonalUse>` already works) and routes the record's own `<Next>` children onto it, instead of creating a fresh non-base entry. Self-healing as a bonus: `BuildSaveData` (`WordDatabase.cs:1116-1144`) already decides `<PersonalUse>` vs `<NewWord>` purely from the runtime `IsFromBase` flag, so the very next save automatically rewrites the now-`IsFromBase=true` entry as `<PersonalUse>` — the stale `<NewWord>` record retires itself with no extra cleanup code. Verified with a new regression test (`OnScreenKeyboardTests.cs`, "WordDatabase — overlay NewWord folds into a matching base entry"): a base file with a real 500-frequency "zult" entry plus a colliding overlay `<NewWord>` (frequency 3, personalUse 7) merges to Frequency=500 (preserved), IsFromBase=true, PersonalUseCount=7 (folded in), and exactly one "zult" in `ByFrequency` (no orphaned duplicate). Full suite: 1716/1716 passing.

- [x] **Priority 1 — `FluentDialogBase` leaks if disposed without `ShowDialog()`** ✓ — Added a `Dispose(bool disposing)` override that shares an idempotent `ReleaseSharedResources()` method with the existing `FormClosed` handler, instead of only cleaning up there. `ReleaseSharedResources()` (guarded by a `_resourcesReleased` flag so it's safe to run from both paths, or twice) unsubscribes `Lang.LanguageChanged`/`SystemEvents.UserPreferenceChanged` and disposes `_tip`/`_err`/`_fontWarn`/`_fontWarnIcon`. `FormClosed` only ever fires for a form that was actually shown (`ShowDialog()`/`Show()`); a form constructed and disposed without ever being shown — the `using var f = new ...Form()` pattern used 15+ times elsewhere in this suite — previously never ran that cleanup at all, permanently leaking the static event subscriptions on every such test run. Verified with a new regression test `T_FluentDialogBase_DisposeWithoutShow` (`OnScreenKeyboardTests.cs`): constructing a form subscribes to `Lang.LanguageChanged` (confirmed via the invocation-list count), disposing it *without* `ShowDialog()` removes that subscription and sets `_resourcesReleased`, and disposing twice doesn't throw. Full suite: 1721/1721 passing.
  Follow-up: fixed the related `KeyEditorForm`-specific instance too. Its `FormClosed` handler (uninstalling the low-level `WH_KEYBOARD_LL` hook used while recording a key sequence, and disposing `_previewFont`) had the identical gap — a form disposed without ever being shown, while a recording happened to be in progress, would leak a hook that intercepts every keystroke system-wide. Extracted to `ReleaseFormResources()`, called from both `FormClosed` and a new `KeyEditorForm.Dispose(bool disposing)` override (which correctly calls `base.Dispose(disposing)`, preserving the `FluentDialogBase` cleanup chain above). Verified with a new assertion in `T_FluentDialogBase_DisposeWithoutShow`: a simulated in-progress recording hook handle (a fake `IntPtr`, not a real installed hook — `UnhookWindowsHookEx` is documented to fail gracefully, not throw, on an invalid handle) is cleared to `IntPtr.Zero` by `Dispose()` alone. Full suite: 1722/1722 passing.

- [x] **Priority 2 — Unhandled exception in `FormClosing` skips shutdown cleanup** ✓ — Re-read the `FormClosing` lambda (`KeyboardForm.cs:375-399`) and `WordDatabase.SaveNow()`/`WriteSaveData` (`WordDatabase.cs:1174,1267`) directly to confirm the finding still held: `SaveNow()` is the synchronous save used specifically at app-close (unlike the periodic `SaveIfDirty()`, which already wraps its background write in its own `try/catch` as "best-effort"), and it calls `WriteSaveData` with no try/catch of its own — an I/O failure there (locked overlay file, full disk, permission denied) propagates straight out of the lambda, aborting everything after it: the window-size capture (`_window.WindowWidth/Height`), `AutoSave()` (the layout save — meaning a resize right before a failed close wouldn't even get the size-save fallback), and the disposal loops for `_lastGearFont`, `_fontCache`, `_cornerFontCache`, and every remaining button's `Region`/handle. Fixed by wrapping just that one call — `try { WordDatabase.SaveNow(); } catch { }` — mirroring `SaveIfDirty`'s existing best-effort pattern in the same file: losing the last ~30 s of learned words (already the documented worst case on a crash) is acceptable, but losing the window/layout state and leaking GDI handles on every failed shutdown is not. Not unit-tested: `KeyboardForm` isn't instantiated anywhere in the test suite (unlike the lighter editor dialogs) since its constructor wires up the toolbar, key buttons, and a low-level `WinEvent` hook — forcing `WriteSaveData` to throw and asserting the rest of a private, inline `FormClosing` lambda still ran isn't practical headlessly. Verified by direct code inspection and tracing the exception path instead, same as the wizard-repaint fix above (Priority 2b).

### Code review (ultra, 2026-08-12) — remaining findings, fixed 2026-09-19 ✓

- [x] **Priority 1 — One failed word-database load permanently disables prediction** ✓ *(correctness, medium)* — `KeyboardForm.LoadWordDatabase` (`KeyboardForm.cs:2451`) set `_lastLoadedDbPath` before the background load's result was known and never cleared it on failure, so a single failed load of the **base `.wfq` file** (e.g. locked by an antivirus scan at startup, or corrupt/half-written) blocked every retry for that path until app restart, leaving prediction off. *Wording corrected:* the original finding named a "locked/corrupt overlay file" as the trigger, but that was inaccurate — `WordDatabase.ApplyOverlay` deliberately swallows overlay errors (`WordDatabase.cs:598-602`), so a bad overlay never fails the load; only a base-file failure (`ParseFile` error → `LoadError` set, nothing published) does. **Fix:** the background load task now releases the guard when the load fails (`!IsLoaded && LoadError != null`, or an exception) via the new `KeyboardForm.ReleaseLoadGuardOnFailure`, an atomic `Interlocked.CompareExchange` that only clears the guard if it still equals the failed path, so a slow failure can't clobber a newer database's guard after a language switch. The next `LoadWordDatabase()` call (e.g. on Apply) then retries. New tests in `OnScreenKeyboardTests.cs`: the guard clears on failure, stays on success, and is left alone when a newer path has replaced it; a corrupt base file leaves `IsLoaded=false`/`LoadError` set, and loading the same path again succeeds once it's readable. Full suite: 1727/1727 passing. *Not changed:* no error message is shown to the user on a failed load (the original finding's "with no error shown" part) — it now recovers on the next reload, but still fails silently.

- [x] **Priority 2 — Edit-mode selection ring can stay stuck on a deselected key** ✓ *(open: needs a manual check)* *(UI bug, medium)* — The ring is painted in `OnButtonPaint` from `_selectedCell`, but nothing repainted a button just because the selection moved; the repaint only happened as a side effect of `BorderSize` changing, and the ring path sets it to 0. For a key whose own border thickness is 0, deselecting set 0 → 0 (no change, so WinForms skipped the repaint) and the old ring stayed drawn; the reverse could also leave the ring undrawn when moving onto such a key. The earlier fix only covered word-prediction cells. **Fix:** new `_ringCell` field records which cell had the ring at the end of the last `RefreshAllButtons()`; after the loop it compares that with the current `_mode == Edit ? _selectedCell : null` and, when they differ, invalidates just those two buttons (new `InvalidateCellButton` helper) and updates the field. One central check covers every selection change (mouse click, arrow keys, leaving Edit mode) instead of patching each `_selectedCell =` assignment, and it repaints only when the ring actually moves. The old WP-only `Invalidate` is now redundant but left in place. **Not unit-tested:** `KeyboardForm` isn't constructed in the suite and a missed repaint isn't observable headlessly, so it's verified by code inspection plus a clean build and the full suite (1727/1727, unchanged). **Manual check:** set a key's border thickness to 0, enter Edit mode, select it, then arrow to a neighbour — the ring should leave the first key and appear on the second.

- [x] **Priority 3 — Duplicate accelerators (Alt+letter) in the editor dialogs** ✓ *(open: needs a visual check)* *(accessibility, low-medium)* — The original finding (Alt+B on both "Border color" and "Border thickness" in the Group Editor) was already gone in English (the thickness label had since become "Thickness (px)"), but a full audit of all three editor dialogs in **both** languages, done by instantiating each dialog and reading every control's mnemonic, found many more clashes: Group Editor (Dutch: E, T), Key Editor (English: F, K, L, O, S, T; Dutch: D, E, G, L, O, T), Keyboard Editor (English: L, O; Dutch: A, L, T). Many labels are built as `"&" + Lang.T(key)`, so their accelerator is the *first letter of the translation* and differs per language — a clash could exist in one language only, which is why the English-only check missed them. The modifier picker label in the Key Editor also shared its key with the "Modifier" mode button, so both carried the same accelerator. **Fix:** a duplicate-free assignment was worked out for all three dialogs in both languages, keeping the current letter wherever it didn't clash and keeping the important mode buttons (Key/Shortcut, Modifier, Send) on their existing letters. Keys whose letter had to change now carry the `&` in the translation (`LanguageManager.cs`, `lang_nl.xml`) and the `"&" +` prefix was removed at their code sites (`Label`, `Layout`, `Key width`, `Key color`, `Border thickness`, `Name`, `Always on top`); the shared `Apply` button is now `P` in Dutch too. The modifier picker label no longer has an accelerator (`StripMnemonic`). *English changes:* Layout Y, Shift label I, Key width E, Font size N, Font color R, Key color Y, Thickness X ("Thickness (p&x)"), Group Editor Name M, Hold-to-edit H, Slow keys E. *Dutch changes:* Label B, Modifier M, Layout Y, Shift send I, Key width D, Key color K, Thickness X, Manage Groups N, Apply P, Always on top B, Sticky keys K, Slow keys N, Timing animation R. Some interior letters (notably X for Thickness, on the "px" unit) are a consequence of the Key Editor having 22 accelerated controls for 26 letters. **Tests:** new `T_Accelerators` (`OnScreenKeyboardTests.cs`) builds each dialog in English and Dutch and fails if any letter is used twice within a dialog (12 assertions); five existing tests that compared the raw translation text now compare it via `Lang.StripMnemonic`, so they don't depend on where the `&` sits. Full suite: 1739/1739 passing. **Not covered:** only the three editor dialogs are audited (the New Group and Import dialogs and the wizard have no clashing accelerators, checked by hand); the main keyboard window has no mnemonics. **Manual check:** open each dialog in both languages and confirm the underlined letters are all different and Alt+letter reaches the intended field.

- [x] **Priority 4 — Unsynchronized `_candidates` field races on language switch** ✓ *(concurrency, low-medium)* — `Load()` runs on a background thread and published four things separately (`_snapshot`, `_candidates`, `_overlayPath`, `_dirty`) while the UI thread read them, so a reader could see one language's candidates paired with the other language's overlay path (bleeding a candidate into the wrong language's overlay file) or lose one. **How real it was:** `Load()` sets `_isLoaded = false` for its whole duration, and `RecordWord`/`PromoteCandidate`/`SaveNow`/`SaveIfDirty` all return early while it is false, so for a real load the window was effectively closed for them; the genuinely exposed callers were `GetCandidates()` and `RemoveCandidate()` (no gate) and tiny databases. The fix makes it structural instead of timing-dependent. **Fix:** the candidate buffer and the overlay path are no longer statics — they are fields of `DbSnapshot` (`Candidates`, `OverlayPath`), assigned by `Load()` before the snapshot is published, so the single volatile `_snapshot = snap` write publishes database, candidates and path together. Every reader captures the snapshot once and uses `snap.Candidates`/`snap.OverlayPath`; `BuildSaveData` now takes the snapshot as a parameter, so the data and the file it is written to always come from the same one. A caller holding a snapshot from before a switch keeps a consistent view of the old language, and nothing can reach the wrong overlay. **`_dirty` companion fix:** `SaveIfDirty` used to clear `_dirty` when the background write *finished*, which also wiped any change made while the write was running (left unsaved until the next change or app close). It now clears the flag *before* taking the copy on the UI thread and sets it back if the write fails; a throw while building the copy also releases `_saving`, which could previously stay stuck. **Tests** (`OnScreenKeyboardTests.cs`, 14 assertions): after each `Load` the snapshot carries its own candidates and overlay path; loading B leaves A's snapshot untouched and `GetCandidates()` shows only B's; `SaveNow` writes only the loaded file's overlay and leaves the other's byte-identical; `SaveIfDirty` clears the flag immediately, a change made during the write stays dirty, and a failed write (a directory where the overlay file should go) leaves it dirty. A mutation check confirmed the two ordering tests fail against the old clear-after-write code. The race itself can't be reproduced deterministically, so the tests pin the structure that rules it out. Full suite: 1753/1753 passing.

- [x] **Priority 5 — Unbounded icon cache with undocumented bitmap ownership** ✓ *(documentation + test, no behaviour change)* *(resource leak, low)* — `SvgIconLoader`'s cache never evicts and hands out shared `Bitmap`s with no stated ownership rule. **Assessment:** the "unbounded" part is real only in theory — the key is (file, tint, size) and the app asks for 34 icons at two sizes (24 px toolbars, 30 px gear) in a handful of tints, i.e. roughly 100–150 bitmaps of ~2 KB (well under half a megabyte), and switching themes re-uses entries once each tint has been seen. The ownership hazard is also only for future code: all five call sites were checked and none disposes an icon, `ToolbarButton.IconImage` is a plain property, and WinForms buttons don't dispose their `Image`. **Fix:** no eviction, deliberately — clearing/disposing on a theme change would leave buttons drawing disposed bitmaps (which throws), and the gear icon is always white while the toolbars follow the theme, so tints legitimately coexist; LRU is pointless for a fixed tiny set; returning copies would cost more memory than the cache saves. Instead `SvgIconLoader.Load` now carries an explicit ownership contract (the bitmap is shared and owned by the cache: same instance for the same key, alive for the session; never `Dispose` it, never draw into or resize it; `Clone()` first if you need one to change or dispose; assigning it to `Button.Image`/`IconImage` is fine), and the cache field documents why it is bounded and that it isn't thread-safe (all callers are on the UI thread). Failures are intentionally not cached, so a transient read error is retried rather than remembered. **Test** `T_SvgIconLoader_Cache` (`OnScreenKeyboardTests.cs`, 10 assertions, using the real icons folder): one cache entry per distinct (file, tint, size); repeated loads return the same instance and never grow the cache; a different tint or size is a different bitmap of the requested size; a missing file returns null and adds nothing. Full suite: 1763/1763 passing.

- [x] **Priority 6 — Empty quoted phrase silently dropped in wizard paste parsing** ✓ *(correctness, low)* — `WizardKeyParser.ParseLine` discarded a closed empty quoted phrase (`if (quoted.Length > 0) row.Add(...)`), so `"" a s d` parsed as three keys instead of four and every key after the gap shifted one column left, with no warning (the wizard preview showed the shifted row, but the intended gap was lost). `""` is the standard way to write an empty value in CSV/spreadsheet exports, which is the likely reason someone types it; the only documented way to leave a gap was `_`. **Fix:** a closed `""` now adds `KeySpec.Blank`, exactly like `_`, so its column is reserved. A lone *unclosed* `"` is deliberately still ignored — the preview re-parses on every keystroke, so half-typed input must not flicker a blank key into the row. `" "` (a quoted space) is unchanged and remains a real key labelled with a space. The syntax notes at the top of `WizardKeyParser.cs` now list the `""` form. A line consisting only of `""` now yields a row with one blank key instead of being skipped. **Tests** (`T_WizardKeyParser`, 15 assertions): `""` at the start, middle and end of a row; a line that is only `""`; consecutive `""`; `""` and `_` produce the same row; the original symptom (`q w e r` over `"" a s d` keeps both rows four wide with keys under their columns); and the neighbouring behaviour that must not change (quoted space, unclosed lone quote, unclosed phrase). Full suite: 1778/1778 passing. **Not addressed (separate limitation):** blank keys can only be written as `_` or `""` because whitespace between tokens collapses, so tab-separated cells pasted from a spreadsheet lose their empty cells the same way (from reading the code; not tried).

- [x] **Priority 7 — Fragile XML-shape assumption in `ConvertWordDb.ps1`** ✓ *(robustness, low)* — The build-time converter (run by the `ConvertWordDb_NL`/`_EN` MSBuild targets) only rewrites the first two lines and assumes line 2 is the whole, non-self-closing `<WordDatabase …>` tag; both real dictionaries satisfy that, but nothing checked it. Running it on eight valid-but-different sources showed 6 of 8 produced malformed XML while exiting 0 (so MSBuild reported success and shipped the file, which the app then skips as corrupt — the language silently vanishes from the choices): a space before `>` inserted the attributes twice (the unbounded regex Replace matched two positions), attributes split over several lines put `<Candidates />` inside the tag, no XML declaration put the attributes on the first `<Word>`, a comment line before the root put them inside the comment, a self-closing root got them after the `/`, and an existing `version` attribute was duplicated. Because MSBuild treats an existing output newer than its source as up to date, the broken file also survived later builds. **Fix (`tools/ConvertWordDb.ps1`):** (1) the attributes are inserted at most once (`Regex.Replace(…, 1)`), which fixes the space-before-`>` case outright; (2) line 2 is validated *before* it is touched (must be the `<WordDatabase` tag, closed on that line, not self-closing, and the source must have two lines) with a message saying what was found; (3) after writing, only the *header* of the output is re-read with an `XmlReader` (root element, the three attributes exactly once, first child `Candidates`, then a `Word`) — not the whole file, so the streaming fast path for huge dictionaries is unchanged; (4) any failure prints an MSBuild-style `error WDB001`, **deletes the bad output** (so the incremental build can't ship a leftover) and exits 1, so the build fails instead of succeeding with a corrupt `.wfq`. Exotic shapes are deliberately *not* converted — a clear build error is enough since the generator's format is fixed. **Verification:** new `tools/Test-ConvertWordDb.ps1` (10 cases: three shapes that must convert, seven that must fail cleanly with no output left, each also given a stale output file) passes against the new script and fails 8/10 against the original from git; the real Dutch dictionary converts to a byte-identical file (14,325,815 bytes) and a normal `dotnet build` regenerates an identical `worddb_NL.wfq`; an end-to-end MSBuild `Exec` on a bad source fails with `error WDB001` + `MSB3073` exit code 1 and leaves no output. The real 206 MB English source (`worddb_EN.xml`, in the working folder but excluded from git) also converts, in about 4 seconds, to a file byte-identical to the existing `worddb_EN.wfq`, so the header check doesn't slow the streaming path. Full suite: 1778/1778 passing (unchanged).

### New keyboard wizard ✓

- [x] 4-page wizard: starting point (blank / paste / copy), grid & labels, theme picker, save. Auto-classifies pasted keys into groups. Four built-in themes. Language selector on page 1. Dark-theme and high-contrast fixes. Nav button positioning. Font-dispose crash fixed.

### Gear button styling (standard group) ✓

- [x] Standard group as style resolution root; gear button styled by it; protected in GroupEditorForm (🔒); reserved name enforcement; "Manage Groups…" button replaces `+`.

### Accessibility ✓

- [x] **Slow keys & dwell click** — Hold-to-register (100–3000 ms) and hover-to-click (100–5000 ms). Amber bottom-up fill animation. Mutually exclusive, saved in `LayoutMeta`.
- [x] **WCAG 2.1 A — Keyboard reachability** — Tab order, arrow-key radio groups, record/browse/list-action buttons all reachable by keyboard.
- [x] **WCAG 2.1 AA — Focus visibility** — 2 px focus ring on all `FluentButton` variants; `ColorSwatchButton` two-tone ring.
- [x] **WCAG 2.1 AA — Colour contrast** — `TextHint` darkened to `#646464` (5.93:1). High-contrast mode support in all dialogs and `FluentButton`/`FluentPainter`.
- [x] **Screen-reader annotations** — `AccessibleName` on every control; tooltips on all three editor forms; `AccessibleDescription` sentinels; preview panel and DataGridView rows fully described.
- [x] **DPI scaling** — All forms `Sizable` + `AutoScroll`; `AutoScaleMode.Dpi`; screen-clamp on load.

### FluentDialogBase ✓

- [x] Shared infrastructure (`AddColorRow`, `AddFieldLabel`, `SetTip`, `AddGroup`, `MakeActionBtn`, `WrapInScrollPanel`, HC live-update, language-change refresh) extracted into `FluentDialogBase`. All four dialog forms inherit it (~300 lines removed).

### Fluent Design / UI overhaul ✓

- [x] `FluentButton` and `ToolbarButton` — owner-drawn, rounded corners, hover/press states, MDL2 icons.
- [x] All three editor forms redesigned (two-column Fluent layout).
- [x] Toolbar: 22 translatable buttons, SVG icons, dark/light/system theme, mode toggles, undo/redo, copy/paste, grid actions.

### Structural & robustness ✓

- [x] `VisualTheme` / `WindowState` / `LayoutMeta` split from `GlobalSettings`.
- [x] Style groups with inheritance chain (Global → Group → Per-key).
- [x] Atomic layout save (`.tmp` + `File.Replace`). Grid validation before write.
- [x] Sparse XML format; DTD/XXE hardening; out-of-bounds clamping.
- [x] Import groups from another layout (conflict table).
- [x] Word prediction: slot auto-assign, renumber on remove, graceful DB failure.
- [x] `Layout` send mode; undo stack (capped at 50).
- [x] Translation system: all toolbar/dialog strings via `Lang.T()`; `lang_nl.xml` complete.

### Bug fixes ✓

- [x] Dead cells after span shrink, resize, expand (3 fixes).
- [x] Window height inflated after closing in Edit mode.
- [x] Groups from one layout persisting after loading another.
- [x] Group style changes via `+` not applied to sibling keys.
- [x] WP duplicate-warning dead code replaced; `NormaliseWPSlots()` on load.
- [x] Test runner console window on WinExe (`AttachConsole` instead of `AllocConsole`).
- [x] `azertycolor.kbl` not typing — `SlowKeysMs` was 300, set to 0.
